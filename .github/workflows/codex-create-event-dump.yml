name: Codex create-event dump

run-name: "Created ${{ github.event.ref_type }}: ${{ github.event.ref }}"

on:
  create:

permissions: {}

jobs:
  dump-event:
    runs-on: ubuntu-latest
    steps:
      - name: Print event payload
        run: jq . "$GITHUB_EVENT_PATH"
